name: Test PR Check
on:
  schedule:
    # 日本時間 08:10 (JST) = 23:10 (UTC)
    - cron: '10 23 * * *'
  workflow_dispatch:      # これを入れておくと、GitHub上のボタンからいつでも手動実行できます

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests PyYAML

      - name: Run check
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          SLACK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: python scripts/secretary.py
